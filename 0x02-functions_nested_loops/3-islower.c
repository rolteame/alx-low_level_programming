#include "main.h"
/**
 * is_lower - check if c is lowercase
 *
 * Return: 1 if lower, 0 if its not
 */
 int _islower(int c)
{
	if (islower(c))
	{
		return (1);
	} else
	{
		return (0);
	}
	_putchar('\n);
}
