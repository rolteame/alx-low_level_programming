#include "main.h"
/**
 * print_most_numbers - print numbers except 2 & 4
 * Return: No return
 */
void print_most_numbers(void)
{
	int num = 48;

	while (num <= 57)
	{
		if (num != 50 && num !- 52)
			_putchar(num);
		num++;
	}
	_putchar('\n');
}
